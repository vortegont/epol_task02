Для корректной работы программы нужно: 
1) Указать путь к файлу БД (возможно потребуется указать корректное имя и пароль для входа на сервер SQL Server)
2) Пересобрать решение

Чтобы узнать о всех существующих командах, введите в консоль команду (help);
Данные из таблиц сохраняются в .txt, но в дальнешем возможно открыть этот файл через Excel.